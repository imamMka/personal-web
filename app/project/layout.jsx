export const metadata = {
  title: "Projects",
  description: "Explore the portfolio of projects by Imam Muttaqin, featuring web applications, UI/UX designs, and fullstack solutions.",
};

export default function ProjectLayout({ children }) {
  return <>{children}</>;
}
